export default function cart(state = [], action) {
  switch (action.type) {
    /* case '@cart/ADD_TO_CART':
      console.tron.log('Chegou no reducer!!!');
      return state; */
    default:
      return state;
  }
}
